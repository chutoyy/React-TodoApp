describe("Mathematic operation", ()=>{
    test("should return 2 if expected 1 + 1 ",() => {
        expect(1+1).toBe(2);
    });

    test("should return the same numer", () => {
        expect(3).toBe(3);
    });
    // test("should return error", () => {
    //     expect(6-3).toBe(5);
    // });

})